import util.Printer;import java.io.IOException;/** * Created by Bernd on 12.11.2015. */public class Main {    public static int MAX_HUB_RESPONSE_SIZE = 6 * 1024; //Optimaler Wert im WIFI laut Messungen. Muss eine Gerade Zahl sein!    public static void main(String... args) throws IOException, InterruptedException {        if (args.length == 1) {            MAX_HUB_RESPONSE_SIZE = Integer.parseInt(args[0]);        }        if (args.length == 2) {            Printer.file = args[1];        }        /* Size muss gerade sein. */        if (MAX_HUB_RESPONSE_SIZE % 2 != 0) {            throw new RuntimeException("Zahl muss gerade sein.");        }        Thread connectionListener = new Thread(new ConnectionListener());        connectionListener.start();        connectionListener.join();    }}