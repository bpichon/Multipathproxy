/** * Created by Bernd on 01.12.2015. */public class MainTest {//    public static void main(String... args) {//        test1(0, 4, 100, 10, 0, 0, "Test 1");//        test1(4, 10, 100, 10, 0, 10, "Test 2");//        test1(4, 14, 100, 10, 0, 10, "Test 3");//        test1(4, 15, 100, 10, 0, 20, "Test 4");//        test1(14, 24, 100, 10, 10, 20, "Test 5");////        for (int i = 0; i < 200; i++) {//            testRange(100, 10, "RangeTest" + i);//        }////        for (int i = 0; i < 200; i++) {//            testRange(98756, 670, "RangeTest" + i);//        }////    }////////    private static boolean test1(int from, int to, int arrayLength, int maxSize, int expectedStart, int expectedEnd, String testName) {//        int[] a = new Container().getStartAndEnd(from, to, arrayLength, maxSize);//        if (a[0] != expectedStart) {//            System.err.printf("%s: Anfang passt nicht. Erwartet: %d | Tatsächlich: %d !\n", testName, expectedStart, a[0]);//        }////        if (a[1] != expectedEnd) {//            System.err.printf("%s: End passt nicht. Erwartet: %d | Tatsächlich: %d !\n", testName, expectedEnd, a[1]);//        }////        return (a[1] == expectedEnd) && (a[0] == expectedStart);//    }//////    private static boolean testRange(int arrayLength, int maxSize, String testName) {//        int totalRange = 0;////        Random random = new Random();//        int currentTo = 0;//        while (currentTo < 100) {//            final int randomNumber = random.nextInt(23);//            int[] r = Container.getStartAndEnd(currentTo, Math.min(currentTo + randomNumber, 100), arrayLength, maxSize);//            currentTo += randomNumber;//            int localRange = (r[1] - r[0]);//            if (currentTo < 100 && localRange % maxSize != 0) {//                System.err.printf("%s: Test failed. localRange: %d !\n", testName, localRange);//                return false;//            }//            totalRange += localRange;//        }//        if (totalRange != arrayLength) {//            // Failed//            System.err.printf("%s: Test failed. Range: %d !\n", testName, totalRange);//            return false;//        } else {//            //System.out.printf("%s: Test bestanden!\n", testName);//            return true;//        }//    }}