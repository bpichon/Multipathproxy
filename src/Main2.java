/** * Created by Bernd on 01.12.2015. */public class Main2 {    /*public static void main(String... args) throws IOException {        HttpLightRequest req = HttpLightRequest.parse(MainTest.r1);        Socket s = new Socket(req.getHost(), req.getPort());        OutputStreamWriter writer = new OutputStreamWriter(s.getOutputStream());        InputStreamReader reader = new InputStreamReader(s.getInputStream());        writer.write(req.toString());        writer.flush();        StringBuilder builder = new StringBuilder();        int length;        char[] buffer = new char[1024];        while ((length = reader.read(buffer)) >= 0) {            builder.append(buffer, 0, length);            System.out.println(builder.toString());        }        s.close();    }*/}